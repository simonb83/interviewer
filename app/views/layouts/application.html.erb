<!DOCTYPE html>
<%# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither %>
<%= html_tag :class => "no-js", :lang => "en" %>
  <%= render "head" %>

  <body class="pages">
    <%= render "chromeframe" %>
    <%= render "header" %>
    <div id="top">
      <% if recruiter_signed_in? %>
        <%= render "nav_recruiter" %>
      <% elsif admin_signed_in? %>
        <%= render "nav_admin" %>
      <% elsif @candidate %>
        <%= render "nav_candidate" %>
      <% else %>
        <%= render "nav" %>
      <% end %>
    </div>
    <div id="flashes">
      <%= render "flashes" %>
    </div>
    <div id="main" role="main">
      <%= yield %>
    </div>
    <%= render "footer" %>
    <%# Javascript at the bottom for fast page loading %>
    <%= render "javascripts" %>
  </body>
</html>